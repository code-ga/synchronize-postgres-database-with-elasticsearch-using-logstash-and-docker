input{
    jdbc{
        jdbc_connection_string => "jdbc:postgresql://10.29.4.1:5432/datasync"
        jdbc_user => "datasync"
        jdbc_password => "root"
        jdbc_driver_class => "org.postgresql.Driver"
        jdbc_paging_enabled => true
        use_column_value => true
        tracking_column => "last_update"
        tracking_column_type => "timestamp"
        schedule => "*/10 * * * * *"
        statement => "SELECT * FROM \"public\".\"Comic\" WHERE last_update > :sql_last_value AND last_update < CURRENT_TIMESTAMP ORDER BY last_update ASC"
    }
}
filter{
    mutate{
        copy => {"id" => "[@metadata][_id]"}
        remove_field => ["@version","@timestamp"]
    }
}
output{
    elasticsearch{
        hosts => ["http://elasticsearch:9200"]
        index => "comic"
        document_id => "%{[@metadata][_id]}"
    }
}